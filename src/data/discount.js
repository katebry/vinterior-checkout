// Set as variables for reusability - if the requirements change, update happens in one clear place.

const PERCENTAGE = 10;
const THRESHOLD = 60;
const BARCODE = "001"
const CHAIRDISCOUNT = 0.75

module.exports = { PERCENTAGE, THRESHOLD, BARCODE, CHAIRDISCOUNT };
